# Agent guidance — mind-judge-config

Read `ARCHITECTURE.md` before editing.

Keep prompt prose public, non-secret, and organized as Markdown by concern.
Use NOTA for manifests. Do not add JSON, YAML, CSV, provider credentials,
private data, or Rust binaries here.
