# skills — mind-judge-config

- Markdown is the prompt prose format.
- NOTA is the manifest/config format.
- Prompt edits should not require rebuilding `mind` or `mind-judge`.
- Never commit secrets, private examples, provider tokens, or live eval output.
