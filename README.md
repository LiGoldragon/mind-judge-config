# mind-judge-config

Public prompt and configuration data for the Mind judge adapter.

Prompt prose lives here so prompt edits do not require Rust or Nix rebuilds of
`mind` or `mind-judge` binaries. The initial layout keeps prompts as Markdown by
concern and indexes them with a NOTA manifest.
